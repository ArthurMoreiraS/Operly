# Neon Development Database
DATABASE_URL=postgresql://user:password@ep-xxx-yyy.region.aws.neon.tech/operly_dev?sslmode=require

# Session (use any value for dev)
SESSION_SECRET=dev-secret-key-for-development

# CORS (local dev)
CORS_ORIGIN=http://localhost:5000

# Email (optional for dev - leave empty to skip email sending)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=
FROM_EMAIL=
APP_URL=http://localhost:5000
