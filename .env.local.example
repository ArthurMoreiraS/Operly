# Local Docker PostgreSQL
DATABASE_URL=postgresql://operly:operly123@localhost:5432/operly_dev

# Session
SESSION_SECRET=dev-secret-key-for-development

# CORS
CORS_ORIGIN=http://localhost:5000

# Email (disabled for local dev)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=
FROM_EMAIL=
APP_URL=http://localhost:5000
